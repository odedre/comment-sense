var test = require('tape')

test('e2e', function (t) {
  t.plan(1)
  // var testCase = `
  //   //identity function
  //   function firstTest(x) {
  //     return x;
  //   }
  // `

  t.equal(true, true)
})
